extends layout
block content
        form(method='post', action='/')
            label(for='username') Username(user):
            br
            input(name='username')
            p
            label(for='password') Password(pass):
            br
            input(name='password' type='password')
            p
            input(type='submit' value='Submit')

        p
        #menu
            a(href='/create') Create A User